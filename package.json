{
  "name": "photoviewer",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://mbborromeo.github.io/react-google-photos-viewer/",
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.0.1"
  },
  "devDependencies": {
    "react-scripts": "3.0.1",
    "node-sass": "^4.12.0",
    "gh-pages": "2.0.0",
    "standard": "^13.1.0"
  },
  "scripts": {
    "format": "standard --fix",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
